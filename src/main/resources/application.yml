server:
  servlet:
    context-path: /senso
spring:
  datasource:
    url: jdbc:h2:mem:sensoDb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console